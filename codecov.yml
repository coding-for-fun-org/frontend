coverage:
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 1%
        base: auto
        # advanced settings
        branches:
          - main
        if_ci_failed: error
        informational: true
        only_pulls: false
    patch:
      default:
        # basic
        target: 80%
        base: auto
        # advanced settings
        branches:
          - main
        if_ci_failed: error
        informational: true
        only_pulls: false
